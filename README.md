# Dot files
- [Setup](#setup)

## Setup
### Gentoo
Install package dependencies
```bash
  > sudo emerge app-admin/ansible
  > ./configure_system.sh
```

### Ubuntu/Debian
Install package dependencies
```bash
  > sudo apt-get install ansible
  > ./configure_system.sh
```
